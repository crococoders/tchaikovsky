import * as React from 'react';
import * as ReactDOM from 'react-dom';
import { Heading } from './components';

ReactDOM.render(
  <div>Electron+react+typecript boilerplate!</div>,
  document.getElementById('root')
);
